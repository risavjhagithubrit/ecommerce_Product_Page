// src/components/Spinner.js
import React from 'react';
import './Spinner.css'; // Add some CSS to style the spinner

const Spinner = () => {
  return <div className="spinner">Loading...</div>;
};

export default Spinner;
